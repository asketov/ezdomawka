@using Common.Consts
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model BLL.Models.ViewModels.FavorSolutionVm



@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<div class="item">
    <div class="col-lg-9 text-center">
        <ul>
            <li>
                <h3>@Model.Theme.Name</h3>
                <span>Об Услуге: @Model.Text</span>
            </li>
            <li>
                <span>
                    <b>Обновлена:</b>
                </span>
                <h6>@Model.Created.ToLocalTime().ToString()</h6>
            </li>
            <li>
                <span>
                    <b>Стоимость:</b>
                </span>
                <h6>@Model.Price rub</h6>
            </li>
        </ul>


        <div class="col-lg-12 col-lg-12--flex">
            <fieldset class="button button--remove">
                <a class="btn btn-outline-warning" asp-action="EditFavor" asp-controller="User" asp-route-id="@Model.Id">Редактировать</a>
            </fieldset>
            
            <fieldset class="button button--ban">
                <a class="btn btn-outline-danger" asp-controller="User" asp-action="DeleteFavor"
                        asp-route-favorId="@Model.Id">Удалить услугу</a>
            </fieldset>
            <fieldset class="button button--contact">
                <button value="@Model.Connection" class="btn btn-outline-secondary" id="contact">Контакты услуги</button>
            </fieldset>
            <fieldset class="button button--contact">
                <button class="btn btn-outline-info" id="reloadFavor" value="@Model.Id">Обновить услугу</button>
            </fieldset>
            <fieldset class="button button--subjects">
                <button value="@Model.Id" class="checkSubjects btn btn-outline-primary">Предметы</button>
            </fieldset>
        </div>
    </div>
</div>